version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: whatsaas-postgres
    restart: always
    environment:
      POSTGRES_USER: whatsaas_user
      POSTGRES_PASSWORD: whatsaas_password
      POSTGRES_DB: whatsaas_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: whatsaas-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
